[package]
name = "fiberish_netstack"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
smoltcp = { version = "0.12", default-features = false, features = ["alloc", "std", "medium-ip", "proto-ipv4", "socket-tcp", "socket-udp"] }
